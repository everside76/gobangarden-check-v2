# 고반가든 MOT 점검 웹앱 V2 — 사용 설명서

체크리스트를 **구글 시트에서 직접 불러오는** 모바일 점검 앱입니다.
문항이나 배점을 바꾸고 싶으면 시트만 고치면 됩니다. 앱을 다시 만들 필요가 없습니다.

👉 **앱 주소: https://everside76.github.io/gobangarden-check-v2/**

### 📱 스마트폰에 설치하기 (앱처럼 사용)

직원들이 위 주소에 접속한 뒤 **⚙️ 설정 → 설치하기** 를 누르면 됩니다.
버튼이 동작하지 않는 브라우저에서는 아래 방법으로 추가하세요.

- **아이폰(Safari)**: 공유 버튼 → "홈 화면에 추가"
- **안드로이드(Chrome)**: 우측 ⋮ 메뉴 → "앱 설치"
- **PC(Chrome·Edge)**: 주소창 오른쪽 설치 아이콘(⊕)

추가하면 고반가든 아이콘을 눌러 일반 앱처럼 실행됩니다. 앱이 수정되면 **새로고침만 하면** 최신 버전이 적용됩니다.

> ⚠️ **카카오톡으로 링크를 보내면** 메신저 안에서 열려 설치가 안 됩니다.
> 이 경우 앱 첫 화면에 빨간 안내가 뜨고, **기본 브라우저로 열기** 버튼을 누르면 Chrome·Safari에서 다시 열립니다.
> 처음부터 그렇게 열도록 직원들에게 안내해 주세요.

---

## 📦 준비물 (이미 만들어져 있습니다)

| 이름 | 용도 | 주소 |
|---|---|---|
| **MOT_고반가든_체크리스트** | 문항·배점·판정 기준 | [열기](https://docs.google.com/spreadsheets/d/1oRr-n4OX9Ue0BX51IN1uJ2trVMB7aDYVsy1woX-cUQE/edit) |
| **MOT_고반가든_점검결과_V2** | 점검 결과가 쌓이는 곳 | [열기](https://docs.google.com/spreadsheets/d/1qMAtiJ8CC0QcL2ijSqV2PFeH1dEZo175E7bFNW4Tu9U/edit) |

---

## 1️⃣ 최초 설정 (한 번만, 약 10분)

### ① 스크립트 붙여넣기

1. **MOT_고반가든_점검결과_V2** 시트를 엽니다.
2. 상단 메뉴 **[확장 프로그램] → [Apps Script]** 클릭
3. 편집기에 있던 `function myFunction() {}` 를 **전부 지우고**,
   `google-apps-script-v2.gs` 파일 내용을 **전체 복사해서 붙여넣기**
4. 💾 **저장** (Ctrl+S)

### ② 필요한 탭 자동 만들기

1. 편집기 상단 함수 목록에서 **`setupSheets`** 선택 → **[실행]**
2. 처음이면 권한 승인 창이 뜹니다
   - 본인 구글 계정 선택 → "Google에서 확인하지 않은 앱" 경고가 나오면
     **[고급] → [(프로젝트 이름)(으)로 이동] → [허용]**
   - *내 시트에서 내 시트로 저장하는 것이라 안전합니다.*
3. 실행이 끝나면 다음 탭이 자동으로 만들어집니다.

| 시트 | 새로 생기는 탭 | 할 일 |
|---|---|---|
| 점검결과_V2 | `점검요약`, `항목상세` | 그대로 두면 됩니다 |
| 체크리스트 | `메타` | 체크리스트 버전이 `v2.0`으로 들어갑니다 |
| 체크리스트 | `10계명` | 10개 항목이 기본값(각 5점)으로 채워집니다 |
| 체크리스트 | `점포명` | **여기에 점포명을 한 줄에 하나씩 적어 주세요** |

### ③ 체크리스트가 잘 읽히는지 확인

1. 함수 목록에서 **`checkChecklist`** 선택 → **[실행]**
2. 하단 **[실행 로그]** 확인 — 이렇게 나오면 정상입니다.

```
체크리스트 버전 : v2.0
문항 수         : 33개
'일반' 만점     : 100점 ✅
단계            : 입점 · 주문 · 서빙 · 그릴링 · 식사중 · 계산 · 환송 · 클레임 · 퇴식
10계명          : 10개
검증            : 통과 ✅
```

> ❌ 표시가 나오면 아래 **문제 해결**을 참고하세요.

### ④ 웹 앱으로 배포

1. 우측 상단 **[배포] → [새 배포]**
2. 톱니바퀴 ⚙️ → **유형 선택: "웹 앱"**
3. 설정
   - **설명**: `MOT V2 수신` (자유)
   - **다음 사용자로 실행**: **나**
   - **액세스 권한이 있는 사용자**: **모든 사용자**
4. **[배포]** → 나오는 **웹 앱 URL** 복사 (`https://script.google.com/macros/s/....../exec`)

> 💡 확인: 복사한 URL을 브라우저 주소창에 붙여넣어
> `"고반가든 MOT V2 수신 서버가 정상 동작 중입니다."` 가 보이면 정상입니다.

### ⑤ 앱에 주소 넣기

**방법 A — 코드에 넣기 (권장, 직원 모두에게 한 번에 적용)**

`js/config.js` 파일을 열어 첫 줄의 `SCRIPT_URL` 에 붙여넣습니다.

```js
SCRIPT_URL: "https://script.google.com/macros/s/....../exec",
```

**방법 B — 앱 화면에서 넣기 (내 기기에만 적용)**

앱 우측 상단 ⚙️ 설정 → **Apps Script 배포 URL** 에 붙여넣고 **저장** → **연결 확인**

---

## 2️⃣ 점검하기

1. **점검 정보 입력** — 지점명·홀 근무인원·피점검자·점검자·점검일·시간
2. **점검 시작하기** → 9단계 33문항을 순서대로 점검

### 문항 종류가 두 가지입니다

**① 일반 문항 (24개, 흰 카드)**

| 버튼 | 뜻 | 점수 |
|---|---|---|
| ○ 이행 | 기준대로 했음 | 배점 전부 획득 (+4, +6 등) |
| △ 미흡 | 했지만 부족함 | 배점의 절반 |
| ✕ 불이행 | 안 했음 | 0점 |
| 해당없음 | **이번 점검에서 확인할 수 없었음** | 채점에서 제외 (분모에서도 빠짐) |

**② 상황발생시 문항 (6개, 노란 카드 ⚡)**

만석 안내·대기 감사·아기 용품·앞치마·뜨거움 주의·클레임 4종·퇴식 3종 — 12가지입니다.

- 기본값은 **"상황 없었음"** 입니다. 그 상황이 없었으면 그냥 두면 됩니다.
- 실제로 그 상황이 있었으면 **"발생함"** 을 누르고 판정하세요.
- **불이행일 때만** 총점에서 −1 또는 −2점이 깎입니다. 이행·미흡은 감점 없습니다.

> 📖 **판정 기준**은 문항마다 항상 펼쳐져 있습니다. 이행/미흡/불이행을 무엇으로 나누는지 바로 보고 판정하세요.
> 화면을 간결하게 보고 싶으면 ⚙️ 설정 → **판정 기준을 접어 두기** 를 켜면 됩니다. (문항별로 눌러서 접었다 펼 수도 있습니다)
> 📝 **비고** 에 특이사항을 적어두면 시트에 함께 저장됩니다.
> 🔵 **매장공통** 배지가 붙은 문항(7·22·26·29·31·32·33번)은 개인이 아니라 홀 전체가 함께 만드는 장면입니다.

3. 모든 문항을 처리하면 **절대 금지 10계명** 화면으로 넘어갑니다. 발생한 항목만 켜고 세부사항을 적으세요. (1건당 −5점)
4. **결과 보기** → 점수·등급·단계별 점수·감점 내역 확인
5. **종합 코멘트** 작성 후 **구글 시트로 결과 전송**

인터넷이 안 되면 결과는 휴대폰에 저장됩니다. **점검 이력 → 🔁 재전송** 으로 나중에 보낼 수 있습니다.

### 📋 점검 이력에서 할 수 있는 일

| 상태 | 결과 보기 | 수정하기 | 재전송 | 삭제 |
|---|:-:|:-:|:-:|:-:|
| **아직 전송 안 함** | ✅ | ✅ | ✅ | ✅ |
| **전송 완료** | ✅ | — | — | — |
| **온라인 이력(구글 시트)** | — | — | — | — |

- 한 번 전송한 점검은 **기록 보존을 위해 수정·삭제할 수 없습니다.**
- 구글 시트에 저장된 기록은 앱에서 열람만 가능합니다. 지워야 한다면 시트에서 직접 행을 삭제하세요.

---

## 3️⃣ 체크리스트 고치기 (문항·배점·기준 변경)

### 시트에서 고치는 방법

1. **MOT_고반가든_체크리스트** 시트의 `체크리스트` 탭을 엽니다.
2. 문항을 고치거나, 행을 추가/삭제합니다.

| 열 | 넣는 값 |
|---|---|
| 순서 | 문항 번호 (숫자) |
| 단계 | 입점 · 주문 · 서빙 · 그릴링 · 식사중 · 계산 · 환송 · 클레임 · 퇴식 |
| 일반구분 | **`일반`** 또는 **`상황발생시`** (정확히 이 두 가지만) |
| 개인구분 | **`개인`** 또는 **`매장공통`** |
| 체크항목 | 점검 문구 |
| 이행/미흡/불이행 기준 | 판정 기준 설명 (비워도 됩니다) |
| 이행시 점수 | 일반: 획득 점수 / 상황발생시: `0` |
| 미흡시 점수 | 일반: 대개 이행의 절반 / 상황발생시: `0` |
| 불이행시 점수 | 일반: `0` / 상황발생시: `-1`, `-2` 처럼 **음수** |

3. ⚠️ **'일반' 문항의 이행시 점수 합계가 반드시 100이어야 합니다.**
   문항을 추가했으면 다른 문항의 배점을 줄여 합계를 100으로 맞추세요.
4. `메타` 탭의 **체크리스트버전**을 올립니다 (예: `v2.0` → `v2.1`).
5. 앱에서 ⚙️ 설정 → **구글 시트에서 다시 불러오기** 를 누르면 즉시 반영됩니다.

> ⏱️ **반영 시점**: 서버가 체크리스트를 10분간 기억하고 있어서, 시트를 고친 직후 직원들 휴대폰에는 **최대 10분 뒤** 반영됩니다.
> 급하게 반영해야 하면 각자 ⚙️ 설정 → **구글 시트에서 다시 불러오기** 를 누르면 즉시 최신본을 받아갑니다.
> (진행 중인 점검은 시작 시점의 문항으로 고정되므로, 점검 도중 문항이 바뀌는 일은 없습니다)

### 고치기 전 확인하는 방법

Apps Script 편집기에서 **`checkChecklist`** 를 실행하면 문제를 미리 잡을 수 있습니다.
`'일반' 만점 : 104점 ❌` 처럼 나오면 배점을 다시 맞춰 주세요.

> 🛟 **안전장치**: 시트에 문제가 있으면 앱은 **직전에 정상이었던 체크리스트로 계속 점검**합니다.
> 시트를 잘못 고쳤다고 해서 현장 점검이 멈추지는 않습니다.

---

## 4️⃣ 구글 시트에 쌓이는 데이터

### `점검요약` 탭 (점검 1건 = 1행)

앞 26개 열은 **V1 결과 시트와 완전히 같은 순서·이름**입니다. V1 데이터와 나중에 그대로 합칠 수 있습니다.

```
전송일시 · 점검ID · 브랜드 · 지점명 · 피점검자 · 점검자 · 점검일 · 요일 ·
점검구분(V2 미사용) · 주문방식(V2 폐지) · 총점 · 등급 · 가산점(V2 미사용) ·
이행 · 미흡 · 불이행 · 미응답 · 10계명위반 · 총항목수 · 종합코멘트 ·
해당없음 · 제외단계 · 원점수 · 만점 · 점검시간 · 홀근무인원
```

그 뒤가 V2 전용 열입니다.

```
점검율 · 점수등급 · 등급상한 · 상황감점 · 상황발생건수 · 10계명감점 ·
개인획득 · 개인만점 · 매장공통획득 · 매장공통만점 · 체크리스트버전 · 체크리스트해시
```

- `원점수`·`만점` = V2의 획득점수·채점 대상 만점입니다 (의미가 이어집니다)
- `체크리스트버전`이 있으면 V2 데이터, 비어 있으면 V1 데이터로 구분됩니다

### `항목상세` 탭 (항목 1건 = 1행)

앞 13개 열이 V1과 동일하고, 뒤에 `배점 · 획득점수 · 일반구분 · 개인구분 · 체크리스트버전`이 붙습니다.
10계명 위반 내역도 함께 기록됩니다.

---

## ❓ 문제 해결

**Q. 앱에 "앱 내장본" 이라고 뜨고 시트 문항이 안 나와요.**
- 설정에서 Apps Script URL이 `.../exec` 로 끝나는지 확인 (`/dev` 아님)
- 배포할 때 액세스 권한을 **"모든 사용자"** 로 했는지 확인
- 설정 → **연결 확인** 을 눌러 오류 메시지를 확인하세요

**Q. `checkChecklist` 에서 `'일반' 만점 : ○○점 ❌` 이 나와요.**
- `체크리스트` 탭에서 `일반구분`이 `일반`인 행들의 `이행시 점수` 합이 100이 아닙니다.
- 오타(`일반 ` 처럼 뒤에 공백), 빈 행, 합계 행이 섞였는지 확인하세요.

**Q. 스크립트를 고쳤는데 반영이 안 돼요.**
- **[배포] → [배포 관리] → 편집(연필) → 버전: 새 버전 → 배포** 를 해야 반영됩니다.

**Q. 점검 이력이 사라졌어요.**
- 이력은 **그 휴대폰의 브라우저**에만 저장됩니다. 캐시를 지우면 함께 사라집니다.
  영구 보관은 구글 시트 전송 또는 CSV 저장을 이용하세요.

**Q. 오프라인에서도 점검할 수 있나요?**
- 됩니다. 한 번이라도 앱을 열었으면 문항이 기기에 저장되어 있어 인터넷 없이 점검할 수 있습니다.
  결과는 기기에 저장되고, 인터넷이 되면 **점검 이력 → 재전송** 으로 보내면 됩니다.

---

## 🧪 채점이 맞는지 확인하고 싶다면

`test/scoring.test.html` 파일을 브라우저로 열면 채점 엔진 테스트 43개가 자동 실행됩니다.
전부 통과(초록)면 계산 로직이 정상입니다.
